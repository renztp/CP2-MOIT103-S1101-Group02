package gui;

import core.DeductionCalculator;
import core.FileHandler;
import core.TimeKeeper;

public class MainFrame {
    public MainFrame(FileHandler fileHandler, TimeKeeper timeKeeper) {
    }
}
